
enum MyEnum {
    None,
    Float(f32),
    Int(i32)
}

struct MyStruct {
    a: MyEnum,
    b: MyEnum
}

fn add_test(a: i32, b: i32) -> i32 {
    return add(a, b);
}

fn random_test(a: i64, b: i64) -> i64 {
    let ret_val: i64 = 0;
    match a {
        1 => match b {
            1 => return 2;
            2 => return 3;
        }
        2 => match b {
            1 => ret_val = 3;
            2 => {
                ret_val = 4;
            }
        }
    }

    let five: i64 = 0;
    while(gt(1000000000, five)) {
        five = add(five, 1);
    }

    /* block comments 
    line
    another line
    */
    /*
    if (gt(five, 5)) {
        five = 5;
    } else {
        five = 1;
    }
    */

    // Just testing syntax
    if gt(5, 5) {
        
    } else if gt(2, 4) {

    }


    return add(ret_val, five);
}
