enum MyEnum {
    None,
    Float(f32),
    Int(i32)
}

struct MyStruct {
    a: MyEnum,
    b: MyEnum
}

fn add_test(a: i32, b: i32) -> i32 {
    return add(a, b);
}

pipe add_structs(a: MyStruct, b: MyStruct) -> MyStruct [{
    return MyStruct {
        a: add_enums(a.a, b.a),
        b: add_enums(a.b, b.b)
    };
}]
