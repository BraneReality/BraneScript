a = 2

my_add = (d: Number, c: Number) => {
    add(d, c) 
}

struct_add = (s) => {
    { a, b } = s
    add(a, b)
}

b = 2

{
    pipe_test: (a: Number, b: Number) => Number [
        my_add,
        (res: Number) => {
            {a: res, b }       
        },
        struct_add
    ]
}
