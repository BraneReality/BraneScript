a = 2
b = 2

{must_be} = Value
d_constraint = must_be(Number)

{must_be} = Value
c_constraint = must_be(Number)

my_add = fn(d: d_constraint, c: c_constraint) { 
    add(d, c) 
}

res1 = my_add(a, b)

struct_add = fn(s) {
    { a, b } = s
    add(a, b)
}

b = 2
struct_add({
    a: res1,
    b
})
